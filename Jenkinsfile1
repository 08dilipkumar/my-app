pipeline {
    agent any 

    stages{
        stage('DOCKER PULL') {
            steps {
                sh 'docker pull 07dilip/app:1'
            }  
        }  
        stage('K8s') {
            steps {
                sh 'sudo kubectl apply -f my-app-k3s.yaml'
            }  
        }    
    } 
}
